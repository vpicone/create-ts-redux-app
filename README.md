## Create TypeScript/Redux App with Prettier

Implements tslint-config-prettier to prevent rule issues between tslint and prettier. Also includes a redux/redux-dev-tools implementation with typescript.
